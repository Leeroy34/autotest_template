const { By } = require("selenium-webdriver");
const { expect } = require("chai");

describe("test description", async function () {
  it("Test number 1", async function () {
    // set data
    const testUrl = "";
    // open page
    await driver.get(testUrl);

    //make actions

    //make assertions
  });
});
